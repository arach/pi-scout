{
  "name": "pi-scout",
  "version": "0.1.0",
  "description": "Scout tools for pi: send, ask, and who — via the local OpenScout broker.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "postinstall": "node ./scripts/setup-host-mcp.mjs",
    "setup:hosts": "node ./scripts/setup-host-mcp.mjs --verbose"
  },
  "files": [
    "broker/",
    "config.ts",
    "scripts/",
    "index.ts",
    "runtime.ts",
    "skills/",
    "target.ts",
    "tools/",
    "types.ts",
    "ui/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arach/pi-scout.git"
  },
  "homepage": "https://github.com/arach/pi-scout",
  "bugs": {
    "url": "https://github.com/arach/pi-scout/issues"
  },
  "keywords": [
    "scout",
    "pi",
    "coordination",
    "agent"
  ],
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": ">=1.0.0"
  },
  "dependencies": {
    "@openscout/protocol": "^0.2.63"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
