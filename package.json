{
  "name": "pi-scout",
  "version": "0.1.0",
  "description": "Scout tools for pi: send, ask, work update, and who — via the local OpenScout broker.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "postinstall": "node ./scripts/setup-host-mcp.mjs",
    "setup:hosts": "node ./scripts/setup-host-mcp.mjs --verbose"
  },
  "files": [
    "broker/",
    "config.ts",
    "scripts/",
    "index.ts",
    "runtime.ts",
    "skills/",
    "target.ts",
    "tools/",
    "types.ts",
    "ui/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arach/pi-scout.git"
  },
  "homepage": "https://github.com/arach/pi-scout",
  "bugs": {
    "url": "https://github.com/arach/pi-scout/issues"
  },
  "keywords": [
    "scout",
    "pi",
    "coordination",
    "agent"
  ],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.78.0",
    "@earendil-works/pi-tui": ">=0.78.0"
  },
  "dependencies": {
    "@openscout/protocol": "^0.2.64"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
